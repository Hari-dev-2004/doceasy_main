services:
  - type: web
    name: doceasy-videocall
    env: python
    pythonVersion: 3.11
    region: oregon  # You can choose a different region
    plan: free  # Change to a paid plan if needed
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 120 --keep-alive 65 --workers 1 wsgi:application
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ENVIRONMENT
        value: production 